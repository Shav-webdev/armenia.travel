import styled from 'styled-components';

const Button = styled.button`
  font: normal normal medium 16px/33px Ping LCG;
  letter-spacing: 0;
  color: #ffffff;
  opacity: 1;
  border: 1px solid #ffffff;
  border-radius: 14px;
  background-color: transparent;
  padding: 8px 24px;
`;
const StyledLang = {
  Button,
};
export default StyledLang;
